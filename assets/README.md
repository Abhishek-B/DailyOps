# Assets

Keep app-owned visual files here when you are ready to move them out of `index.html`.

Suggested use:
- `images/` — logos, venue backgrounds, icons not embedded as SVG
- `css/` — extracted stylesheet later
- `js/` — extracted application modules later

For customer/venue-uploaded logos in production, prefer Supabase Storage rather than committing them to this repository.
